<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>avsh.specialist_mx</groupId>
  <artifactId>Specialist_MX</artifactId>
  <name>Specialist MX</name>
  <version>1.7.0</version>
  <description>The emulator of the computer "${project.name}"</description>
  <scm>
    <connection>scm:git:https://github.com/${GitHubUsername}/${project.artifactId}.git</connection>
    <developerConnection>scm:git:https://github.com/${GitHubUsername}/${project.artifactId}.git</developerConnection>
    <url>https://github.com/${GitHubUsername}/${project.artifactId}.git</url>
  </scm>
  <build>
    <resources>
      <resource>
        <targetPath>${project.build.outputDirectory}/${resources.dir}</targetPath>
        <directory>${project.build.sourceDirectory}/${resources.dir}</directory>
        <includes>
          <include>**/*.rom</include>
          <include>**/*.png</include>
        </includes>
      </resource>
      <resource>
        <targetPath>${project.build.outputDirectory}/${resources.dir}</targetPath>
        <filtering>true</filtering>
        <directory>${project.build.sourceDirectory}/${resources.dir}</directory>
        <includes>
          <include>**/*.properties</include>
        </includes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.0.2</version>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>buildnumber-maven-plugin</artifactId>
        <version>1.4</version>
        <executions>
          <execution>
            <phase>validate</phase>
            <goals>
              <goal>create</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <doCheck>false</doCheck>
          <doUpdate>false</doUpdate>
          <format>{0}.{1,number}</format>
          <items>
            <item>${project.version}</item>
            <item>buildNumber</item>
          </items>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.6.1</version>
        <configuration>
          <source>${jdkVersion}</source>
          <target>${jdkVersion}</target>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.0.2</version>
        <configuration>
          <archive>
            <manifest>
              <packageName>${project.groupId}</packageName>
              <mainClass>${project.groupId}.Main</mainClass>
            </manifest>
            <manifestEntries>
              <Implementation-Build>${buildNumber}</Implementation-Build>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.0.0</version>
        <executions>
          <execution>
            <id>copy-dependencies</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <includeScope>compile</includeScope>
              <outputDirectory>${project.build.directory}/${lib.dir}</outputDirectory>
              <overWriteReleases>false</overWriteReleases>
              <overWriteSnapshots>false</overWriteSnapshots>
              <overWriteIfNewer>true</overWriteIfNewer>
              <useRepositoryLayout>false</useRepositoryLayout>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.0.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <mainClass>${project.groupId}.Main</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.akathist.maven.plugins.launch4j</groupId>
        <artifactId>launch4j-maven-plugin</artifactId>
        <version>1.7.16</version>
        <executions>
          <execution>
            <id>l4j-clui</id>
            <phase>package</phase>
            <goals>
              <goal>launch4j</goal>
            </goals>
            <configuration>
              <headerType>gui</headerType>
              <outfile>${project.build.directory}/${project.artifactId}.exe</outfile>
              <jar>${project.build.directory}/${project.artifactId}-${project.version}.jar</jar>
              <errTitle>${product.name}</errTitle>
              <downloadUrl>http://java.com/download</downloadUrl>
              <priority>normal</priority>
              <icon>${project.artifactId}.ico</icon>
              <classPath>
                <mainClass>${project.groupId}.Main</mainClass>
                <addDependencies>false</addDependencies>
                <preCp>anything</preCp>
              </classPath>
              <jre>
                <path>./jre</path>
                <minVersion>${jdkVersion}.0</minVersion>
              </jre>
              <versionInfo>
                <fileVersion>${buildNumber}</fileVersion>
                <txtFileVersion>${buildNumber}</txtFileVersion>
                <fileDescription>${project.description}</fileDescription>
                <copyright>${copyright}</copyright>
                <productVersion>${buildNumber}</productVersion>
                <txtProductVersion>${buildNumber}</txtProductVersion>
                <productName>${product.name}</productName>
                <companyName>${company.name}</companyName>
                <internalName>${project.artifactId}</internalName>
                <originalFilename>${project.artifactId}.exe</originalFilename>
                <trademarks>-= AVSh Software =-</trademarks>
                <language>RUSSIAN</language>
              </versionInfo>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <properties>
    <GitHubUsername>AVSh-Soft</GitHubUsername>
    <resources.dir>avsh/specialist_mx/resources</resources.dir>
    <company.name>"AVSh Software" (Alexander Shevtsov)</company.name>
    <copyright>Copyright © 2017 ${company.name}</copyright>
    <lib.dir>lib</lib.dir>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jdkVersion>1.8</jdkVersion>
    <product.name>The emulator "${project.name}"</product.name>
  </properties>
</project>

